import styled from 'styled-components';

export const Container = styled.div`
    align-items: center;
    display: flex;
    font-size: 14px;
    padding-bottom: 20px;

    .input {
        border: none;
        flex-grow: 1;
        margin-left: 3px;

        input {
            font-size: 14px;
        }
    }
`;