import React from 'react';
import { MainEmptyContainer } from './styles';

export const MainEmpty: React.FC = () => {
    return (
        <MainEmptyContainer>
            empty
        </MainEmptyContainer>
    );
};
